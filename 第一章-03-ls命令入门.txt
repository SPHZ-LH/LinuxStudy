1.ls命令的作用是？

    在命令行中，以平铺的形式，展示当前工作目录（默认HOME目录）下的
    内容（文件或文件夹）

2.HOME目录是？

    每一个用户在Linux系统的专属目录，默认在：/home/用户名

3.当前工作目录？

    Linux命令行在执行命令的时候，需要一个工作目录，打开命令行程序（
    终端）
    默认设置工作目录在用户的HOME目录