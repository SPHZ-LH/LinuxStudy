1.Linux系统的超级管理员用户是：root用户

2.su命令

    · 可以切换用户，语法：su [-] [用户名]

    · -表示切换后加载环境变量，建议带上

    · 用户可以省略，省略默认切换到root

3.sudo命令

    · 可以让一条普通命令带有root权限，语法：sudo 其他命令
    
    · 需要以root用户执行visudo命令，增加配置方可让普通用户有sudo命令的执行权限